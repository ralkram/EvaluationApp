@model IEnumerable<EvaluationApp.Domain.Evaluation>


<br /><br />
<div class="container">
    <div class="row">
        <div class="table-responsive">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>Employee Name</th>
                        <th>Evaluation Form</th>
                        <th>Employee</th>
                        <th>Last Evaluator</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody id="myTable">
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.EvaluationName</td>
                            <td>@item.FormName</td>
                            <td>@item.Employee</td>
                            <td>@item.LastEvaluator</td>
                            <td>
                                <img src="~/images/start.png" />
                                <img src="~/images/cancel.png" />

                            </td>
                        </tr>
                    }

                </tbody>
            </table>
        </div>
        <div class="col-md-12 text-center">
            <ul class="pagination pagination-lg pager" id="myPager"></ul>
        </div>
    </div>
</div>